Names:
        Brooke Thompson, utln: bthomp05
        Ian Ryan, utln: iryan01
Acknowledgements:

    TAs- Dan, Bill, Zane, Trevor

    Bryant and O’Hallaron
    Chapter 6 of Bryant and O’Hallaron helped us better understand caches.

    Stack Overflow
    Used other people’s questions and answers to help apply them to our own 
    questions and solve similar problems in our code.

    Given bin files
    Used cat on given files to help understand functions 
    we are given and can use.

Implemented successfully:
        We believe that all of files, programs, and functions are implemented
        correctly and should function as expected.

Architecture Documentation:
    Uses a 2D blocked array for block major and a 2D array for row and 
    column major. ppmstrans will make an array type depending of which specified
    type the user inputs. Then it will call function pointers on this new array 
    and uses uarrayblocked or uarray2d functions to iterate through a file that 
    is read and create a new array. This array will be used in a map function 
    that will iterate the file by block-major, row-major, or col-major and at 
    each index it will apply a function. Each apply function puts the element 
    at each index into a new index of a new array in order to make a rotated 
    file by 180 or 90 or 0. Then the new array is put into the first file and is
    read into a pnm reader that then is able to convert the pix map to an iamge.

Table:
-------------------------------------------------------------------------------
|               | size of images used | total CPU time | time per input pixel |
-------------------------------------------------------------------------------
|block rotate 0 |                     |                |                      |
-------------------------------------------------------------------------------
|block rotate 90|                     |                |                      |
-------------------------------------------------------------------------------
|block rotate180|                     |                |                      |
-------------------------------------------------------------------------------
| row rotate 0  |                     |                |                      |
-------------------------------------------------------------------------------
| row rotate 90 |                     |                |                      |
-------------------------------------------------------------------------------
|row rotate 180 |                     |                |                      |
-------------------------------------------------------------------------------
|col rotate 0   |                     |                |                      |
-------------------------------------------------------------------------------
|col rotate 90  |                     |                |                      |
-------------------------------------------------------------------------------
|col rotate 180 |                     |                |                      |
-------------------------------------------------------------------------------
Name of computer:
Model of computer:
CPU type: 
Clock rate:

Rough hours spent: 22.